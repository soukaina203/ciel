{% extends 'base.html.twig' %}

{% block title %}Sujet index{% endblock %}

{% block body %}
    <h1>Sujet index</h1>
<h3> 
  <a href="{{ path('reponses')}}" > acceder au reponses </a>

 </h3>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Contenu</th>
                <th>IdUser</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sujet in sujets %}
            <tr>
                <td>{{ sujet.id }}</td>
                <td>{{ sujet.titre }}</td>
                <td>{{ sujet.Contenu }}</td>
                <td>{{ sujet.idUser }}</td>
                <td>
                    <a href="{{ path('app_sujet_show', {'id': sujet.id}) }}">show</a>
                    <a href="{{ path('app_sujet_edit', {'id': sujet.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_sujet_new') }}">Create new</a>
{% endblock %}
